// Canonical support inbox — reused everywhere a "contact us" address is shown
// (contact page, checkout success, legal pages) so there's a single source of truth.
export const SUPPORT_EMAIL = 'hola@kairo.es'
